{
  "name": "atproto-agents",
  "version": "0.1.0",
  "private": true,
  "description": "Agent identity & communication on the AT Protocol, authorized by OpenFGA — demonstrated by AI agents playing Codenames",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @atproto-agents/lexicon && npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "slides": "marp slides/slides.md -o slides/dist/slides.html",
    "slides:pdf": "marp slides/slides.md -o slides/dist/slides.pdf"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
