node_modules/
dist/
*.tsbuildinfo
# Secrets. Broad on purpose: this repo is public and infra/.env holds live
# PDS admin passwords, the PLC rotation private key, and API keys. Only
# .env.example is ever committed.
.env
.env.*
!.env.example
*.env
.local/
*.pem
*.key
slides/dist/
coverage/
.DS_Store

# Generated QR codes for the talk (regenerate with the starter-pack script)
qr-codes/

# Talk artifacts, kept out of the repo on purpose: generated QR codes and the
# local excalidraw→pptx export tool (both reproducible, neither reviewed in a diff).
qr-codes/
slides/excalidraw-export/
